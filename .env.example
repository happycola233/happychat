# 后端端口
PORT=8787
# 数据与上传文件目录
DATA_DIR=./data
# SQLite 路径（drizzle-kit 也会读取）
DATABASE_URL=./data/happychat.db
# 会话签名密钥（生产必须设置为高强度随机值）
SESSION_SECRET=
# 由可信边缘设置且剥离用户输入的客户端 IP 请求头；通常留空
CLIENT_IP_HEADER=
# 信任的 X-Forwarded-For 代理跳数；默认 0，填大于实际跳数会允许伪造 IP
TRUSTED_PROXY_HOPS=0
