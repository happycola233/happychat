开展工作前，请先阅读 `./docs/architecture.md` 与 `README.md`。

深度思考，积极搜索互联网查阅最新开发文档（尤其是 OpenAI API 文档）与行业最佳实践。
确保代码可维护性，避免回归，拒绝代码冗余与「屎山」堆积。
命名及时重构，新变量命名需语义清晰，同时更新相关的旧变量名。
代码意图不明确时，主动编写清晰准确的中文注释。
网页 UI 的调整需同时兼顾浅色与深色模式。
需要任何工具或依赖，请自行安装。
临时文件统一放在 `.tmp/` 目录（该目录已加入 `.gitignore`，不存在时自行创建）。
代码编写完毕后，需要时同步更新相关文档：`./docs/architecture.md` 与 `README.md`
未被 `.gitignore` 忽略的文件不得包含真实用户聊天内容或敏感数据。

如需使用 LLM API 冒烟或登录账号进行测试，相关凭据请查阅：`.devfiles/TESTING.md`。

被要求写 commit 时，只撰写 conventional commit 文案，不要执行 commit 和 push（除非用户提到），
内容应当描述最后一个「最后一个 commit → 当前工作区」的整体 diff，
而不应该提及本次实现过程中的尝试、报错、排查、返工或中间修正的迭代过程。