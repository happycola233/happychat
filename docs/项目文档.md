# happychat 工程文档（交接用）

面向接手开发者，不是宣传材料。每节尽量落到具体文件、函数、命令和风险点。阅读顺序建议：先看 §1、§2 跑起来，再看 §5（生成链路）这是全项目最复杂、最容易改坏的部分。

---

## 1. 这是什么 / 当前状态

私有 AI 聊天站。服务端把浏览器请求转成对「OpenAI 兼容上游」的 **Responses API**（文本/思考/联网/视觉/文件）与 **Images API**（生图）调用，流式回传。

- 已实现并验证：P0–P9 全部（鉴权+邀请码、Provider/模型后台、流式聊天、SSE 断线续传、对话分支、思考摘要、联网引用、图片/文件输入、gpt-image-2 生图、Markdown/LaTeX、管理后台统计/用户/邀请码/日志、暗色主题、生产单体托管）。
- 验证方式：`scripts/` 下 13 个脚本（Playwright 浏览器 E2E + 直连后端冒烟），全部对一个真实的 OpenAI 兼容上游（Base URL/Key 在 `.env` 或后台配置，不写进代码）跑通。
- `npm run typecheck` / `lint` / `test`(9 个单测) / `build` 全绿。
- **仓库尚无任何 git commit**。`data/` 与 `.env` 已在 `.gitignore`。

代码量：`server` 40 个 `.ts`、`web/src` 53 个 `.ts/.tsx`、`shared` 10 个。

---

## 2. 本地运行与命令

### 2.1 命令（`package.json` scripts）
| 命令 | 作用 | 备注 |
|---|---|---|
| `npm run dev` | 同时起后端+前端 | `concurrently` 跑 `dev:server` + `dev:web` |
| `npm run dev:server` | `tsx watch server/index.ts` | 后端 **8787**，改动自动重载 |
| `npm run dev:web` | `vite` | 前端 **5173**，代理 `/api`→`127.0.0.1:8787` |
| `npm run typecheck` | `tsc --noEmit` 分别跑 server/web 两个 tsconfig | |
| `npm run lint` | `eslint .` | |
| `npm run test` | `vitest run` | 仅 `{server,shared}/**/*.test.ts` |
| `npm run build` / `build:web` | `vite build` → `dist/web` | 后端不需要构建，直接 `tsx` 运行 |
| `npm run start` | `NODE_ENV=production tsx server/index.ts` | 静态托管 `dist/web` |
| `npm run db:generate` | `drizzle-kit generate` | 改 `schema.ts` 后生成迁移 |
| `npm run db:migrate` | `tsx server/db/migrate.ts` | 也会在每次后端启动时自动执行 |
| `npm run db:seed` | 确保已迁移；若设了 `UPSTREAM_*` 则**幂等**预置一个 Provider 并同步模型，否则跳过 | `server/db/seed.ts` |

### 2.2 首次使用流程
1. `npm install`（`better-sqlite3@12` 在 Node24/Windows 走预编译二进制，无需 VS 构建工具）。
2. `npm run dev`，打开 `http://localhost:5173`。
3. 注册页提示「首位用户将成为管理员，无需邀请码」→ 注册即管理员。
4. 进「管理后台 → 提供商」加上游（Base URL + Key）→ 测试连接 → 同步模型。
5. 「模型」页调能力/默认参数/思考等级；回聊天即可用。

### 2.3 Windows 本地的两个坑（已写入 `docs` 与记忆）
- **关进程**：`tsx watch` / `vite` 由 `npm` 拉起，杀 `npm` 不会杀到孙进程。要按端口杀：
  `BP=$(netstat -ano|grep ':5173'|grep LISTENING|awk '{print $NF}'); taskkill //F //T //PID $BP`（Git Bash 里 `//F` 是对 `/F` 的转义）。
- **curl 传中文**：Git Bash 命令行里内联中文给 `curl -d` 会乱码（应用本身 UTF-8 正常）。测试要把 JSON 体用 Node 写到 `data/body.json` 再 `curl --data-binary @data/body.json`；`/tmp` 在 Node 看来是 `C:\tmp`（不存在），共享文件放 `data/`。

---

## 3. 仓库结构与模块职责

单仓库、非 monorepo。`shared/`（前后端共享类型与 zod schema）、`server/`、`web/`，靠 TS 路径别名 `@shared/*` 串起来。

### 3.1 别名解析（容易踩）
- tsc：每个叶子 tsconfig（`server/tsconfig.json`、`web/tsconfig.json`）各自声明 `paths: { "@shared/*": ["../shared/*"] }`，**无 `baseUrl`**（TS6/7 已废弃 baseUrl，加了会报错）。
- 运行时 tsx：读**根** `tsconfig.json` 的 paths。
- Vite：`vite.config.ts` 里手写 `resolve.alias`（不依赖插件）。
- vitest：`vitest.config.ts` 里手写 alias。
- **`server/db/schema.ts` 是唯一例外**：用相对路径 `../lib/id`、`../../shared/types/domain`，因为 `drizzle-kit` 不解析 `@shared` 别名。改这个文件时别改回别名，否则 `db:generate` 挂。

### 3.2 shared/
- `types/domain.ts`：领域类型 + JSON 列类型。关键：`ContentPart`（消息内容部件联合：`input_text`/`output_text`/`input_image`/`input_file`/`image_result`）、`ModelCapabilities`、`ModelParams`（含 `image` 选项）、`ReasoningEffort`、`MessageUsage`、`RunState`。
- `types/events.ts`：SSE 线格式 `WireEvent {type,seq,data}`、`RUN_EVENT_TYPE`（合成事件名）、`TERMINAL_EVENT_TYPES` + `isTerminalEventType()`。**前后端都依赖它判断终止**。
- `types/api.ts`：所有 HTTP DTO（`PublicUser`/`ModelDTO`/`AdminModelDTO`/`ConversationDTO`/`MessageDTO`/`SendResult`/`AttachmentDTO`/`InviteCodeDTO`/`AdminUserDTO`/`StatsDTO`/`ErrorLogDTO`/`UsageLogDTO`/`ApiError`）。
- `schemas/*.ts`：zod 请求校验（`auth`/`model-config`/`chat`/`admin`），同时导出推断类型供前端用。`chat.ts` 的 `sendMessageSchema` 含 `.refine`（text 或 attachments 至少一个）。

### 3.3 server/
- `index.ts`：启动入口。顺序：`runMigrations()` → `recoverInterruptedRuns()` → 建 `Hono<AppEnv>` → 挂 `/api/*` 路由 → 生产环境 `serveStatic('./dist/web')` → `notFound`（`/api` 返 JSON 404，其余生产环境回 `index.html` 做 SPA 回退）→ `onError`（`UpstreamError`→502 友好中文）→ `serve(8787)`。
- `env.ts`：`loadEnv()` 用 zod 校验 `process.env`，生产环境若 `SESSION_SECRET`/`APP_ENCRYPTION_KEY` 仍是 `dev-` 默认值则**直接退出**。
- `db/client.ts`：`better-sqlite3` + drizzle，开机设 `PRAGMA journal_mode=WAL / synchronous=NORMAL / foreign_keys=ON / busy_timeout=5000`，自动建 `data/` 目录。导出 `db`、`sqlite`。
- `db/schema.ts`：13 张表（见 §4）。
- `db/migrate.ts`：`runMigrations()` 用 drizzle migrator 跑 `db/migrations/`；也能 `tsx server/db/migrate.ts` 单独跑（用 `pathToFileURL` 判断是否被直接执行）。
- `lib/id.ts`：`newId()`(uuidv7) + `genInviteCode()`(8 位、去 I/O/0/1)。
- `lib/crypto.ts`：`encryptSecret`/`decryptSecret`（AES-256-GCM，密钥 = `sha256(APP_ENCRYPTION_KEY)`）、`maskSecretTail`。**Provider API Key 落库前必经此处**。
- `lib/assert.ts`：`must(x)` —— insert().returning() 必然返回行的断言，去掉一堆 `!`。
- `auth/`：`password.ts`(scrypt，零原生依赖) / `session.ts`(签名 cookie `hc_session` + DB `sessions` 表，30 天) / `middleware.ts`(`requireUser`/`requireAdmin`) / `users.ts`(`toPublicUser`)。
- `http/types.ts`：`AppEnv = { Variables: { user } }`、`AuthUser = users.$inferSelect`。
- `http/validator.ts`：`jsonValidator(schema)` 包 `@hono/zod-validator`，校验失败返回中文。
- `provider/`：上游适配，见 §6。
- `runs/`：生成核心，见 §5。
- `storage/files.ts`：本地文件存储，见 §9。
- `services/`：`models.ts`/`conversations.ts`/`admin.ts`/`providers.ts`（`syncProviderModels` 供后台同步与 `db:seed` 复用）—— DB 查询 + DTO 映射，路由层只编排不写复杂 SQL。
- `routes/`：`auth`/`admin`/`models`/`conversations`/`runs`/`attachments`，见各节。

### 3.4 web/src/
- `main.tsx`：挂 `QueryClientProvider`，引入 `katex` 与 `highlight.js/styles/github-dark.css`。
- `App.tsx`：`RouterProvider` + `Toaster` + 主题 `applyTheme` 副作用。
- `router.tsx`：守卫 `RequireAuth`/`RequireAdmin`/`RedirectIfAuthed`；聊天路由 `/` 与 `/c/:id` 共用 `ChatLayout`；后台 `/admin/*` 共用 `AdminLayout`。
- `api/`：`client.ts`(fetch 封装 + `ApiRequestError` + `apiUpload` 多部分) + 各域 API。
- `hooks/`：`useAuth`(useMe/useLogin/useRegister/useLogout) / `useModels` / `useConversations`。
- `store/`(zustand)：`chat.ts`(用户偏好，**persist 到 localStorage**：模型/联网/思考/图片选项) / `stream.ts`(按会话 id 存活动流) / `theme.ts` / `toast.ts`。
- `sse/`：`eventReducer.ts`(纯函数折叠事件→`LiveMessage`) / `streamManager.ts`(EventSource 生命周期)。
- `chat/`：聊天 UI，见 §7。
- `pages/`：`LoginPage`/`RegisterPage` + `admin/*` 9 个页面。

---

## 4. 数据库设计（`server/db/schema.ts`）

### 4.1 约定（为了将来迁 PostgreSQL）
- 主键 `text` + UUIDv7（`pk()` 工厂，`$defaultFn(newId)`）。
- 时间 `integer({mode:'timestamp_ms'})`（不用 SQLite 的 `CURRENT_TIMESTAMP`）。
- 布尔 `integer({mode:'boolean'})`。JSON `text({mode:'json'}).$type<T>()`。
- 不用任何 SQLite 专有特性。迁移产物在 `db/migrations/0000_careful_gwen_stacy.sql`。

### 4.2 表与关键点
- `users` / `invite_codes` / `sessions` / `user_settings`：账号体系。注意 `user_settings.theme` 列已存在但前端主题实际存 localStorage（见 §12）。
- `providers`：`api_key_encrypted` 是密文。`models`：`capabilities`/`default_params`/`hard_params`/`allowed_efforts` 都是 JSON；`kind` ∈ `responses|image`；`UNIQUE(provider_id, model_id)`。
- `conversations`：`active_leaf_id` 指向当前可见分支叶子（**无 DB 级外键**，避免与 messages 循环引用，应用维护）。`system_prompt_override` 列存在但无 UI。
- `messages`（**合并了节点树与内容，单表**）：`parent_id` 自引用构成分支树（无 DB 外键）；`content` 是 `ContentPart[]`；`run_id`、`reasoning_summary`、`annotations`、各 token 列、`error_message`。索引 `(conversation_id, parent_id)` 支撑树遍历。
- `attachments`：`storage_path` 是绝对路径；`message_id` 上传后才回填（无 DB 外键）。
- `runs`：状态机（见 §5），`idempotency_key UNIQUE`、`last_sequence_number`、`assistant_message_id`（无 DB 外键）。
- `run_events`：`UNIQUE(run_id, sequence_number)` —— 续传游标 + 去重的命门。`data` 是去 `obfuscation` 后的事件，**绝不存图片 b64**（只存文件引用）。
- `usage_logs` / `error_logs`：FK 多为 `set null`，所以**删用户/会话后日志仍在**（统计审计用）。`usage_logs` 冗余存 `model_label`/`provider_label`。

### 4.3 外键级联策略
层级关系（sessions→users、models→providers、messages→conversations、run_events→runs 等）用 `onDelete:'cascade'`；循环/自引用的 4 个列（`messages.parent_id`、`conversations.active_leaf_id`、`runs.assistant_message_id`、`messages.run_id`）**无 DB 外键**，靠应用保证。日志表 FK 用 `set null`。

---

## 5. 生成核心链路（最复杂，重点读）

涉及文件：`runs/prepare.ts` → `runs/manager.ts` → `runs/engine.ts`(文本) / `runs/image-run.ts`(图片) → `runs/finalize.ts` → `runs/emitter.ts` → `routes/runs.ts`(SSE) → 前端 `sse/streamManager.ts` + `sse/eventReducer.ts` + `chat/ChatView.tsx`。

### 5.1 发起（`routes/runs.ts` `POST /api/runs` → `prepare.ts`）
`prepareRun()`：
1. `getRunnableModel(modelId)` 取模型+Provider（都启用才行）。
2. 校验附件归属与能力（图片需 `vision`、文件需 `file_input`），不通过返 400。
3. 没 conversationId 就建会话。
4. 建 user 消息（parent = `args.parentId ?? conv.activeLeafId`；**编辑重发时传 `parentId` 使其成为兄弟分支**）。
5. `createAssistantAndRun()`：建 assistant 占位消息（status `streaming`）+ run（state `queued`）→ **立刻把 `conversation.active_leaf_id` 指到新 assistant**（关键：否则刷新时这一回合不在可见路径上，无法续传）→ 按 `model.kind` 构建 body：`responses` 走 `buildResponseBody` + `buildInput`（含附件 data URL 解析），`image` 走 `buildImageBody`（prompt = 路径最后一条 user 文本）。

路由拿到 `prepared` 后 `runManager.start(...)` **异步**跑引擎，立即返回 `{runId, conversation, userMessage, assistantMessage}`。`/regenerate` 走 `prepareRegenerate()`（在原 user 消息下加兄弟 assistant，不建新 user 消息）。

### 5.2 引擎（`runs/manager.ts` 按 `model.kind` 分派）
- `runManager.start()`：建 `AbortController` 存进 `active` Map，按 kind 调 `runEngine` 或 `runImageEngine`，`.finally` 时从 Map 删除。
- `runEngine`（`engine.ts`，文本）：`persistEmit(type,data)` 每个事件**先写 `run_events`（同步 better-sqlite3）再 `runEmitter.emit`**，自增 `seq`。先发合成 `run.created`，置 run `running`；然后 `for await` 消费 `client.createResponseStream(body, signal)`，逐事件 persistEmit + 累积 text（delta）/reasoning（summary delta）/annotations/usage（completed）；循环结束或抛错后调 `finalizeRun`。中止（signal.aborted）→ state `canceled`。
- `runImageEngine`（`image-run.ts`，图片）：合成 `run.created`/`image.generation.in_progress` → `createImage` → b64 落盘为 attachment（关联 assistant 消息）→ 合成 `image.generation.completed{attachmentId}` → 内联 finalize（assistant content = `[{type:'image_result',...}]`，usage 记 `image_tokens`）。**非流式，无 partial_images**（见 §12）。

### 5.3 终结（`runs/finalize.ts`，文本路径）
单次 CAS：`UPDATE runs SET state=? WHERE state IN ('queued','running')`（防重复 finalize）。写 assistant 消息最终 content/状态/usage/reasoning/annotations、`usage_logs`、失败时 `error_logs`、再 `persistEmit` 终止事件（`run.done`/`run.error`/`run.canceled`）。`status` 映射：completed→`complete`，failed→`error`，incomplete/canceled→`interrupted`。

### 5.4 SSE 续传协议（`routes/runs.ts` `GET /api/runs/:id/stream`，全项目最易写错处）
游标 `effectiveFrom = max(?from, Last-Event-ID)`。`streamSSE` 回调里：
1. **先订阅** `runEmitter`（实时事件进队列），再查 DB `run_events where seq>from`（**先订阅后回放**避免漏事件）。
2. 回放完若已见终止事件→关闭。
3. 否则查 run 状态：若已终态且 `!runManager.isActive`（引擎已结束或启动中断）→ 补查剩余事件，**没有终止行就按 run.state 合成一个**（启动中断的 run 没有持久化终止事件）。
4. 否则进实时循环：从队列取、`seq<=lastSeq` 去重、写出、遇终止事件 `return`。
- 客户端断开 → `stream.onAbort` 置 `aborted`，**不**中止后台生成。
- 第三参数 onError 吞掉写入异常（客户端掉线）。

### 5.5 启动恢复（`runs/manager.ts` `recoverInterruptedRuns`）
进程启动时把所有 `queued/running` 的 run 置 `interrupted`，对应 assistant 消息置 `interrupted`。**这是「无 worker/Redis」的代价**：进程一重启，内存里的 `active` Map 和 `runEmitter` 订阅全没了，正在跑的生成无法接续，只能标记中断。

### 5.6 前端消费（`sse/streamManager.ts` + `eventReducer.ts` + `ChatView.tsx`）
- `startStream({runId,conversationId,assistantMessageId,fromSeq,onTerminal})`：开 `EventSource('/api/runs/:id/stream?from=N', {withCredentials})`，`onmessage` 解析 `WireEvent`、`reduceEvent` 折叠进 `useStreamStore.byConversation[convId]`、更新 `lastSeq`、遇终止事件 `finish()`；`onerror` 自管退避重连（`1s→30s`，`MAX_ATTEMPTS=6`，超限置 `interrupted`）。**不靠浏览器自动重连**（fresh 连接不带 Last-Event-ID，所以总带 `?from`）。
- `ChatView.tsx` 是编排中枢：`onSend`/`onEdit`(带 parentId)/`onRegenerate`/`onSwitch`；`applyRunResult` 把新消息塞进 react-query 缓存并 `startStream`；`useEffect([id])` 做 **resume-on-load**（`getActiveRun` 查到未完成 run 就续传）；另一个 `useEffect` 做**交接**（流终止且 DB 消息已最终化 → `clearStream`，从内存流切回持久化内容）。
- `Message.tsx` 渲染时，若该 assistant 消息 id == 当前流的 `assistantMessageId` 就用 `live` 覆盖（流式文本+光标/思考卡/生成中图片）。

---

## 6. 上游 API 适配（`server/provider/`）

所有上游事实是**实测 + OpenAI SDK 类型双重确认**的（见项目记忆 `upstream-responses-api-contract`）：`store` 默认 false（故本地重放，不发 `previous_response_id`）；流事件带 `sequence_number`；`output_text.delta` 有干扰字段 `obfuscation`（必剥）；思考等级合法值 `low/medium/high/xhigh`；错误信封 `{error:{message,type,code,param?}}`，未知模型上游返 **502**。

- `client.ts` `ProviderClient`：唯一对上游 fetch 的地方。`endpoint()` 用 `joinBaseUrl`（base 末尾已是 `/v1`，**禁用 `new URL(path,base)` 否则丢 /v1**，有单测 `shared/util/url.test.ts`）。方法：`listModels`/`createResponse`/`createResponseStream`(去 obfuscation)/`createImage`。`providerClientFromRow` 解密 Key 构造。
- `params.ts` `buildResponseBody`：参数优先级 **硬参数 > 用户请求 > 模型默认 > 代码默认**。思考按 `allowed_efforts` 校验后才发 `reasoning.effort`；联网仅 `capabilities.web_search && 开关` 才挂 `tools:[{type:'web_search'}]`；开思考时 `max_output_tokens` 兜底 ≥25k（`REASONING_MIN_OUTPUT_TOKENS`）；最后 `mergeDeep(hardParams)`（如 `reasoning.summary='auto'`）；强制 `store:false`。`buildImageBody` 类似但走 size/quality/background。
- `sse-parse.ts` `parseSSEStream`：按 `\n\n` 切块、按 `data.type` 标注、剥 `obfuscation`，处理跨块拼接与 `\r\n`/`[DONE]`。有单测。
- `normalize.ts`：`parseResponse`(非流式 Response → 文本/引用/思考/usage)、`mapUsage`、`buildAssistantContent`。
- `context.ts` `buildInput`：把分支路径消息转成上游 `input[]`。**助手历史用 `output_text`(annotations:[])**，用户用 `input_text/input_image/input_file`；图片/文件用传入的 `attachments` Map 在此刻读成 base64 data URL。
- `errors.ts`：`UpstreamError` + `friendlyUpstreamMessage`（按 `error.type` 而非 HTTP 码映射中文）+ `networkError`。全局 `onError` 把它转 502 + 友好 message。
- `model-defaults.ts` `inferModelDefaults`：同步模型时按 id 猜默认（含 image→image kind、gpt-5→reasoning + `summary:auto` 硬参数）。

---

## 7. 前端架构（`web/src`）

### 7.1 状态分层
- 服务端状态：**TanStack Query**（`['me']`/`['models']`/`['conversations']`/`['conversation',id]`/`['admin',*]`）。
- 客户端持久偏好：**zustand persist** `store/chat.ts`（localStorage `happychat-prefs`）。
- 流式实时态：`store/stream.ts`（按 conversationId，**支持多会话并发流**）。
- 主题：`store/theme.ts`（localStorage `happychat-theme`）。

### 7.2 聊天组件（`web/src/chat/`）
- `ChatLayout`(Sidebar+Outlet) / `ChatView`(编排，见 §5.6) / `Sidebar`(会话列表+新建+删除+管理入口+登出+主题切换)。
- `Composer`：自增高文本框、附件上传（按 `canImage/canFile` 显示图片/文件按钮）、底部控件槽 `leftControls`、发送/停止按钮。Enter 发送、Shift+Enter 换行。
- `Message`：用户气泡(右) vs 助手(左、Markdown)；`live` 覆盖流式态；hover 操作区（复制/编辑/重新生成）；`branch` 分支切换 `‹n/total›`；附件渲染。
- `Markdown`：`react-markdown` + `remark-gfm`/`remark-math` + `rehype-katex`/`rehype-highlight`，**不开 `rehype-raw`**（故原始 HTML 当文本转义，安全）；`PreBlock` 给代码块加复制按钮（读 `textContent`）；表格包横向滚动；`memo` 化避免流式每 token 全量重渲。
- `ReasoningCard`：组件内计时器（每 200ms）+ 思考中自动展开/完成自动折叠 + 自动滚底。
- `ChatControls`：按 `model.kind` 与 `capabilities` 分派——文本模型显示联网开关 + 思考等级；图片模型显示尺寸/质量。
- `buildPath.ts`：客户端从 `activeLeafId` 沿 `parent_id` 构建可见路径 + `getSiblings`（与服务端 `services/conversations.ts` 的 `buildPath`/`deepestLeaf` 对应）。

---

## 8. 鉴权与权限

- 会话：登录建 `sessions` 行 + 签名 httpOnly cookie `hc_session`（`auth/session.ts`）。**用 cookie 而非 Bearer**，正是为了让 `EventSource` 能自动带凭证做续传。
- 中间件：`requireUser`/`requireAdmin`（`auth/middleware.ts`），把 `user` 放进 `c.var.user`。
- 邀请注册：`routes/auth.ts` 的 `POST /register` 在**单事务**里查重→校验/兑换邀请码（`used_count<max_uses`、未过期未停用）→建 user+user_settings。**首位用户（users 表为空）免邀请码且自动 admin**。`GET /bootstrap` 告诉前端是否首用户。
- ⚠ 没有独立的 `auth/invites.ts`，兑换逻辑内联在 register 里（计划里提过该文件，实际内联了）。
- 普通用户只能看自己数据：会话/消息查询都带 `userId` 过滤（`services/conversations.ts getOwnedConversation`）；附件读取校验 `userId`（`routes/attachments.ts`）；run 流校验归属（`getOwnedRun`）。
- 管理员自我保护：`routes/admin.ts` 禁止改/删自己（`id===c.get('user').id`→400）。

---

## 9. 附件与图片生成（`server/storage/files.ts` + `runs/image-run.ts`）

- 上传 `POST /api/attachments`（`routes/attachments.ts`，`c.req.parseBody()` 取 `File`）：判 `isImageMime`→kind，限大小（图 20MB/文件 32MB），落盘 `data/uploads/<uuid><ext>`，建 `attachments` 行，返回 `AttachmentDTO`。
- 读取 `GET /api/attachments/:id`：校验 `userId` 后 `new Response(buf)` 内联返回（`<img src>` 走同源带 cookie）。
- 发送时不内联 base64 进 DB，`messages.content` 只存 `attachment_id`；`prepare.ts resolveAttachments` 在**构建请求那一刻**用 `toDataUrl` 读盘转 base64（图 `input_image.image_url`、文件 `input_file.file_data+filename`）。
- 生图：`gpt-image-2` 走 `/images/generations`（实测返 `data[].b64_json`），`runImageEngine` 落盘成 attachment 关联 assistant 消息，`content` 存 `image_result`，前端 `Attachments.tsx` 渲染（`alt="生成的图片"`、`title=revised_prompt`）。

---

## 10. 管理后台

- 后端：`routes/admin.ts`（全 `requireAdmin`）+ `services/admin.ts`（统计聚合用 drizzle `sql` 求和分组）+ `services/models.ts`。覆盖 Provider CRUD/测试/同步、模型 CRUD、邀请码 CRUD、用户改角色/禁用/删除、`getStats`、`listErrorLogs`/`listUsageLogs`。
- 前端：`web/src/pages/admin/`，`AdminLayout` 侧边导航 7 项（统计/提供商/模型/用户/邀请码/错误日志/系统设置）。`ModelEditor.tsx` 是较大的表单（能力开关、思考等级多选、默认参数、系统提示词）。

---

## 11. 测试与验证（`scripts/` + vitest）

- 单测（`npm run test`）：`shared/util/url.test.ts`、`server/lib/crypto.test.ts`、`server/provider/sse-parse.test.ts`（共 9 个）。
- E2E/冒烟脚本（需先 `npm run dev`，再 `npx tsx scripts/<x>.ts`）：
  - 后端直连：`resume-test`(游标续传)、`abort-test`(停止)、`file-input-test`、`image-gen-backend`、`final-smoke`(全模型)。
  - Playwright 浏览器：`ui-smoke`、`stream-resume-e2e`(流式+刷新续传)、`branch-e2e`、`reasoning-web-e2e`、`image-input-e2e`、`image-gen-e2e`、`markdown-e2e`、`admin-e2e`。
- 这些脚本里的 `data-testid`（`assistant-message`/`stop-btn`/`edit-textarea`/`edit-submit`）是给 E2E 用的，改动相关组件时注意别删。
- ⚠ 这些脚本**没有接进 CI / 没有自动清库**，反复跑会往 `data/happychat.db` 累积测试会话/用户。需要干净状态时删 `data/happychat.db*` 重新 `npm run dev`（会自动迁移建表，但会丢已配置的 Provider，要重配）。

---

## 12. 已知限制 / 风险点 / TODO（务必读）

按严重度大致排序：

1. ~~`server/db/seed.ts` 缺失~~ **已修复**：已实现 `server/db/seed.ts`（读 `UPSTREAM_*`、幂等预置 Provider + 同步模型），同步逻辑抽到 `services/providers.ts` 与后台共用，`.env.example` 描述已订正。
2. **单进程续传**：重启即中断（§5.5）；`runEmitter`/`active` Map 在内存，**不支持多实例水平扩展**。要扩展得引入持久化事件总线（与「不用 Redis」的约束冲突，需重新决策）。
3. ~~ESLint 未装 `eslint-plugin-react-hooks`~~ **已修复**：`eslint.config.js` 已对 `web/**` 启用 `react-hooks`（rules-of-hooks=error、exhaustive-deps=warn）与 `react-refresh`；原有警告已正确消除（`invalidateDetail` 用 `useCallback` 稳定化、`textFromContent` 拆到 `contentText.ts`、路由守卫拆到 `guards.tsx`），无任何 disable 压制。
4. **图片生成非流式**：`runImageEngine` 一次性 `createImage`，生成中只显示 spinner，没有 `partial_images` 渐进。要渐进得改成 Images API `stream:true` 或 Responses 的 `image_generation` 工具，并扩 `eventReducer` 处理 `partial_image`。
5. **附件内联 base64**：跳过 Files API（兼容性好），但请求体大、且每轮重放都重发；长 PDF/多图会显著增大上下文与延迟。无大小预算告警。
6. **无分页**：会话列表、会话内消息、日志（`limit 100`）都不分页；`run_events` 永不主动清理（随会话级联删除）。长期重度使用下 `messages`/`run_events` 会膨胀。
7. **会话级系统提示词只有后端**：`conversations.system_prompt_override` 列与 `prepare.ts` 已用它，但前端没有设置入口；当前系统提示词只能在「模型默认系统提示词」里配。
8. **主题持久化双轨**：实际存 localStorage（`store/theme.ts`），`user_settings.theme` 列闲置；多设备不同步。要同步需接 `user_settings` 读写 API（当前无）。
9. **CSRF**：仅靠 `SameSite=Lax` cookie；所有写操作是同源 JSON fetch，没有显式 CSRF token。私有站可接受，但若将来开放需补。
10. **Markdown 流式重解析**：每个 delta 重新 parse（已 `memo`，但长文仍有开销）；半截 markdown（未闭合代码块）会中途渲染错乱再纠正。
11. **构建单 chunk 偏大**（~390KB + katex/hljs），有 vite 警告；未做代码分割（如把 admin、markdown 懒加载）。
12. **标题**：仅 `text.slice(0,30)`，无智能标题（计划里的 `services/title.ts` 没实现）。
13. **better-sqlite3 原生模块**：Windows 用预编译；迁 Ubuntu 时 `npm ci` 会按目标平台重编（需基础构建链或对应预编译）。换 Node 大版本也可能要 `npm rebuild`。

---

## 13. 常见扩展怎么做

- **加一种内容能力**（如音频输入）：扩 `shared/types/domain.ts ContentPart` → `provider/context.ts buildInput` 映射 → `prepare.ts` 能力校验 → 前端 `Composer` 上传 + `Attachments` 渲染 + `models` 能力标记。
- **加管理后台页**：`shared/types/api.ts` 加 DTO → `server/services/admin.ts` 加查询 → `server/routes/admin.ts` 加路由 → `web/src/api/admin.ts` 加调用 → `web/src/pages/admin/` 加页面 → `AdminLayout` 导航 + `router.tsx` 路由。
- **改/加上游参数**：只动 `server/provider/params.ts`（别在别处散落 fetch）；新字段先确认上游真支持（参考 §6 的实测纪律，别凭猜）。
- **迁 PostgreSQL**：换 `drizzle.config.ts` dialect 与 `db/client.ts` 驱动为 `drizzle-orm/node-postgres`；schema 列类型基本可直接用（JSON→jsonb、timestamp_ms→timestamp、boolean）；`db.transaction` 在 better-sqlite3 是同步、PG 是异步，`routes/auth.ts` 注册事务与 `runs/*` 里的同步 `.run()` 调用需改成 await 形态。

---

## 14. 关键文件速查

| 想改… | 看这个文件 |
|---|---|
| 上游请求参数 | `server/provider/params.ts` |
| 上游事件解析/去 obfuscation | `server/provider/sse-parse.ts` |
| 生成状态机/续传 | `server/runs/engine.ts`、`finalize.ts`、`routes/runs.ts` |
| 分支/编辑/重发 | `server/runs/prepare.ts`、`server/services/conversations.ts`、前端 `chat/ChatView.tsx` |
| 数据表 | `server/db/schema.ts` |
| 前端流式渲染 | `web/src/sse/streamManager.ts`、`sse/eventReducer.ts`、`chat/Message.tsx` |
| Markdown/代码/公式 | `web/src/chat/Markdown.tsx` + `web/src/index.css` 的 `.hc-md` |
| 鉴权/邀请码 | `server/routes/auth.ts`、`server/auth/*` |
| 部署 | `README.md`、`server/index.ts` 末尾静态托管段 |
