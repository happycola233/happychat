/**
 * 应用图标（设置「关于」页与登录 / 注册页共用）。
 *
 * 查询串必须与 `web/index.html`、`web/public/manifest.webmanifest` 保持一致，
 * 否则换图后浏览器仍会命中旧图标的缓存。换图时三处需同步更新。
 */
export const APP_ICON_SRC = '/app-icon-192x192.png?v=5abed49c'
